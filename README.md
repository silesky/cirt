* This project is using SystemJS for imports and Babel for es6. 
* I prefer jspm for smaller projects, as it's simpler to configure than webpack.
* Development:
    * `npm install -g jspm`
    * `jspm install`
    * `live-server .`